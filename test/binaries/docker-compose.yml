services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        HOST_UID: ${UID}
        HOST_GID: ${GID}
    user: "${UID}:${GID}"
    volumes:
      - .:/src
    command: make
